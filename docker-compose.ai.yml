# ================================
# DnarMasID — AI Infrastructure
# ================================

services:
  ollama:
    image: ollama/ollama:latest
    restart: unless-stopped
    environment:
      OLLAMA_HOST: 0.0.0.0:11434
      OLLAMA_MODELS: /root/.ollama
      TZ: ${TZ:-Asia/Jakarta}
    volumes:
      - ollama-data:/root/.ollama
    networks:
      - dnarmasid-net

networks:
  dnarmasid-net:
    driver: bridge

volumes:
  ollama-data:
